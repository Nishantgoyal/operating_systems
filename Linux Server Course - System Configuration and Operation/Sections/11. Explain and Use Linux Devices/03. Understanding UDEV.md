# Understanding UDEV

- It is the _User-Space Device Manager_
  - It uses _sysfs_
    - _sysfs_ is a virtual filesystem
    - it contains information about the hardware
  - It also follows _rules_
    - The _rules_ are intended to keep device name consistent
    - The _rules_ are located in `/etc/udev/rules.d` folder
